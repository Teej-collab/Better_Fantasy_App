# Native Migration Feature Matrix — Weekend League

Companion to `docs/NATIVE_MIGRATION_AUDIT.md`. One row per major feature area.
"Migration difficulty" and "Risk" are qualitative (Low/Medium/High), not numeric scores,
per the audit brief. "Recommended migration phase" refers to the sequence in
`NATIVE_MIGRATION_AUDIT.md` §16.

| Feature | Current location | Current implementation | Business logic location | UI location | Backend dependency | Web-specific? | Native replacement needed? | Can share code? | Migration difficulty | Risk | Recommended phase |
|---|---|---|---|---|---|---|---|---|---|---|---|
| Auth (email/password) | `frontend/src/app/login`, `backend/app/routers/auth.py`, `backend/app/auth/` | JWT (HS256) + bcrypt; Bearer-header path already built for native | Backend (`auth/session.py`, `routers/auth.py`) | Frontend login form (web-specific JSX) | Yes — fully | No (backend already native-ready) | UI only (native login screen + secure token storage) | Backend auth logic: 100%. UI: no. | Low | Low | Phase 2 |
| Auth (Discord/Google OAuth) | `backend/app/routers/auth.py`, `frontend/src/app/auth/complete` | Redirect-based OAuth; token handed off via URL fragment to a browser page | Backend (OAuth exchange, state-CSRF) | Web-only completion page | Yes | Yes — completion flow assumes a browser | Yes — deep link/custom scheme or in-app browser session | Backend OAuth exchange logic: yes. Completion UI: no. | Medium | Medium | Phase 1 (backend deep-link target) + later phase (native UI) |
| Session/cookie relay (`api/backend` proxy, `auth/ticket`) | `frontend/src/app/api/backend/[...path]`, `frontend/src/app/auth/ticket` | Cookie-relay proxy to dodge Safari ITP; ticket JWTs for WS/uploads | N/A (web-specific plumbing) | N/A | Partial (ticket endpoint is backend, reusable) | Yes — the proxy itself is entirely a web workaround | No — native just uses Bearer header directly, simpler than web | Ticket-minting endpoint: yes, via Bearer header instead of cookie. Proxy itself: not needed. | Low (native path is simpler, not harder) | Low | Phase 2 |
| Standings / matchups / playoffs / power rankings | `frontend/src/app/(app)/{standings,matchups,power-rankings}`, `backend/app/routers/league.py` | Server-rendered pages fetching REST JSON | Backend (`domain/`) | Web JSX | Yes | UI only | Yes (native screens) | API client (`lib/api.ts` equivalents): high. UI: no. | Low | Low | Phase 3 |
| Roster / lineup management | `frontend/src/app/(app)/team`, `backend/app/routers/me.py`, `domain/lineup_engine.py` | REST GET/POST, no drag-and-drop (button-based moves) | Backend | Web JSX | Yes | UI only | Yes | Business logic (lineup rules): 100% backend, reusable. UI: no, but no DnD library needed either (buttons port easily). | Low–Medium | Low | Phase 3 |
| Free agents / waivers / trades / keepers | `frontend/src/app/(app)/{free-agents,trades,keepers}`, `backend/app/routers/{trades,keepers,free_agents}.py`, `domain/waivers.py` | REST CRUD | Backend | Web JSX | Yes | UI only | Yes | High (backend logic), UI no | Medium | Low–Medium | Phase 3 |
| Players / player cards | `frontend/src/app/(app)/players`, `components/players/` | REST + client-side search/filter | Backend (`routers/players.py`) | Web JSX + modal (`PlayerCardProvider` context) | Yes | UI only | Yes | High | Low | Low | Phase 3 |
| History / awards / season awards | `frontend/src/app/(app)/{history,seasons}` | REST | Backend (`domain/` awards logic) | Web JSX | Yes | UI only | Yes | High | Low | Low | Phase 3 |
| Draft room | `components/draft/DraftRoom.tsx`, `backend/app/draft/`, `routers/draft.py` | WebSocket (ticket-auth) for live state; click-based queue (no DnD) | Backend (draft engine) + portable WS-lifecycle logic in `DraftRoom.tsx` | Web JSX, 774-line client component | Yes | Partially — WS protocol is portable, JSX is not | Yes — native screen + native WebSocket client | WS protocol/reconnect logic: high. Business logic (pick eligibility, queue order): backend, 100%. UI: no. | Medium–High | Medium | Phase 4 |
| Chat | `components/chat/ChatApp.tsx`, `backend/app/chat/`, `routers/chat.py` | WebSocket (ticket-auth), typing/read-receipts, `visibilitychange` reporting, clipboard-paste image upload | Backend + portable WS logic | Web JSX | Yes | Partially — clipboard-paste and `visibilitychange` are web-specific | Yes — native screen, `AppState` instead of `visibilitychange`, image-picker instead of clipboard paste | WS protocol: high. Read-receipt/typing logic: high. UI + 2 browser-API touchpoints: no. | Medium | Medium | Phase 4 |
| Gamecast (live game tracking) | `components/gamecast/GamecastShell.tsx`, `backend/app/gamecast/`, `routers/gamecast.py` | WebSocket + REST fallback (4s server poll of Sportradar/mock) | Backend | Web JSX | Yes | UI only | Yes | WS protocol: high. Backend: 100%. | Medium | Low–Medium | Phase 4 |
| Presence (site-wide online/offline) | `components/PresenceProvider.tsx`, backend presence tracking | WebSocket, mounted once globally | Backend + portable WS logic | React Context (mostly portable) | Yes | Minor (visibility reporting) | Yes — native WS client, `AppState` | High | Low–Medium | Low | Phase 4 |
| Watch Party (LiveKit audio/video) | `components/watchparty/`, `backend/app/watch_party/`, `routers/watch_party.py` | LiveKit web SDK/components; backend mints scoped JWT per room | Backend (token issuance): 100% reusable | Web JSX using `@livekit/components-react` (web-only) | Yes | Yes — UI package is web-only | Yes — `@livekit/react-native` SDK, custom UI | Token-issuance logic: high. UI: no, needs full rebuild against RN SDK. | High | Medium | Phase 6 |
| Push notifications | `backend/app/notifications/`, `routers/push.py`, `frontend/src/lib/push.ts`, `public/sw.js` | Web Push / VAPID only (`pywebpush`) | Backend (needs new work) | Web-only subscribe flow | Yes — needs new backend work | Yes — VAPID/PushManager has zero native compatibility | Yes — `expo-notifications` + new APNs/FCM backend dispatch path, additive | Preference/dedup/rate-limit pipeline (`formatter.py`/`events.py`): design reusable, storage schema is not. | High | High (new backend work + new client work, both required) | Phase 1 (backend) + Phase 5 (client) |
| Admin dashboard | `components/admin/`, `frontend/src/app/(app)/admin`, `backend/app/routers/admin.py` | Client-rendered, polling (20s/60s), custom charts | Backend | Web JSX | Yes | UI only | Native version optional/lower priority (commissioner-only tool) | High (backend) | Low–Medium | Low | Not in initial phases — defer |
| Chug analyzer | `components/ChugUpload.tsx`, `backend/app/chug_analyzer/` | Multipart video upload (ticket-auth), server-side CV pipeline (separate Python 3.11 venv) | Backend (100% — CV logic never needs to be client-side) | Web `<input type="file">` + streamed-heartbeat response parsing | Yes | Yes — upload UI is web-specific; response-parsing logic is portable | Yes — `expo-image-picker`/camera capture (arguably a UX improvement over web) | Heartbeat-stream response parsing: high. Upload UI: no. | Medium | Low | Phase 6 |
| Theming / appearance settings | `globals.css`, `components/settings/{AppearanceSection,LabsSection}.tsx` | `document.cookie` + CSS custom properties + inline boot script | N/A (web-specific mechanism) | Web-only | No | Yes — entirely | Yes — native settings store (e.g. `AsyncStorage` or a small context) + a native theme/token system | Design tokens (`--wl-*` values) are portable as data even though the CSS mechanism is not. | Medium | Low | Phase 2–3 (bundled with initial theming decisions) |
| Home dashboard card reordering | `components/HomeCardDeck.tsx` | `@dnd-kit` drag-and-drop | N/A | Web-only | No | Yes | Yes — `react-native-gesture-handler`/`reanimated` or a draggable-list package | Low (small, self-contained feature) | Low | Low | Phase 3 |
| Bottom-nav reordering | `components/settings/NavigationSection.tsx` | `@dnd-kit` drag-and-drop | N/A | Web-only | No | Yes | Same as above | Low | Low | Low | Phase 3 |
| League logo upload/crop | `components/settings/LogoUploadCropper.tsx` | Canvas-based client-side cropping + Vercel Blob upload | N/A | Web-only (`<canvas>`) | Yes (upload token) | Yes | Yes — native image-crop library | Low | Medium | Low | Phase 3 |
| Analytics (`page_view`/`feature` events) | `frontend/src/lib/analyticsEvents.ts`, `backend/app/analytics/` | `page_view` auto-classified by URL route; curated `feature` events | Backend ingestion: 100% reusable. Classification: route-based (web assumption). | N/A | Yes | Partially — route-based classification assumes a URL bar | Yes — small taxonomy extension for screen-based (not route-based) events | Ingestion endpoint: high. Event taxonomy: needs extension, not rebuild. | Low | Low | Phase 1 (taxonomy) then ongoing |
| PWA / service worker | `public/manifest.json`, `public/sw.js`, `ServiceWorkerRegistration.tsx` | Web Push display + network-first API caching | N/A | N/A | No | Yes — entirely | No — moot once truly native (its push-handling logic is a useful reference only) | N/A | N/A | N/A | Not applicable — superseded by native push |
| Existing Capacitor wrapper | `frontend/capacitor.config.ts`, `frontend/ios/`, `frontend/android/` | WebView pointed at the live production site (`server.url`), no bundled build | N/A | N/A (wraps the whole web app) | No new dependency | N/A — it's the current "native" answer | Superseded by, not a dependency of, an RN migration | N/A | N/A | N/A | Open question — see audit §18, not a migration phase itself |
